# swift-fixtures

A Swift package that provides fixtures for Swift types through a macro system.

## Development

```bash
make build    # Build project
make test     # Run tests
make format   # Format code
make lint     # Lint code
make clean    # Clean build artifacts
make help     # Show all commands
```